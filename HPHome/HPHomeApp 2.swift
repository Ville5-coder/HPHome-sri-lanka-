import SwiftUI

// NOTE: This is no longer the app entry point to avoid duplicate @main conflicts.
// Use your existing app file as the @main entry and set HomeView() as its root if desired.
struct HPHomeRootView: View {
    var body: some View {
        HomeView()
    }
}
#Preview {
    HPHomeRootView()
}

